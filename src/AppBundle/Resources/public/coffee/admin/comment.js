// Generated by CoffeeScript 1.12.7
var $collectionHolder, addAllActionLinks, addItemFormCheckboxAction, addItemFormDeleteLink;

$collectionHolder = null;

jQuery(document).ready(function() {
  $collectionHolder = $('ul.mutable-items');
  return $collectionHolder.find('li.list-item').each(function() {
    return addAllActionLinks($(this));
  });
});

addAllActionLinks = function($itemFormLi) {
  addItemFormDeleteLink($itemFormLi);
  return addItemFormCheckboxAction($itemFormLi);
};

addItemFormDeleteLink = function($itemFormLi) {
  var $removeFormA, $removeItemLink;
  $removeItemLink = $('<a href="#" class="btn btn-default">delete comment</a>');
  $removeItemLink.on('click', function(e) {
    e.preventDefault();
    return $itemFormLi.remove();
  });
  $removeFormA = $('<div class="text-right"></div>').append($removeItemLink);
  return $itemFormLi.prepend($removeFormA);
};

addItemFormCheckboxAction = function($itemFormLi) {
  var $activateFormA;
  $activateFormA = $itemFormLi.find("input:checkbox");
  return $activateFormA.on('change', function(e) {
    return $itemFormLi.toggleClass("deactivated");
  });
};

$(document).ready(function() {
  return $("#edit-commens").on('submit', function(event) {
    event.preventDefault();
    $('ul.mutable-items').find("li.list-item .comment-label").each(function() {
      return replaceSymbols($(this));
    });
    $('ul.mutable-items').find("li.list-item .comment-author").each(function() {
      return replaceSymbols($(this));
    });
    return $(this).unbind('submit').submit();
  });
});
