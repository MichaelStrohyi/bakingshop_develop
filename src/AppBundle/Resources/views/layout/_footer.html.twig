{#<div class="footer-top"></div>#}
<section class="footer-bottom">
    {% for menu in menus %}
        <div class="footer-menu">
            {{ menu.type == constant('AppBundle\\Entity\\Menu::TYPE_BOTTOM') ? render(controller('AppBundle:Page:menu', {'name': menu.name})) }}
        </div>
    {% endfor %}
</section>